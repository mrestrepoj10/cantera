# Real APS credentials used by the live Viewer token route. The embedded OAuth
# demo seeds the same client values locally, so its sign-in flow stays isolated.
APS_CLIENT_ID=
APS_CLIENT_SECRET=
APS_VIEWER_DEMO_URN=
APP_ORIGIN=

# Optional outside the showcase defaults.
APS_AUTH_BASE_URL=
SESSION_SECRET=
