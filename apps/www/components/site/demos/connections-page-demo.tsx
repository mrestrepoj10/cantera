import { ConnectionsViewDemo } from '@/components/site/demos/connections-view-demo'

// The template mounts the same view over acc-auth-routes; the docs preview
// shows the view driven by sample data.
export function ConnectionsPageDemo() {
  return <ConnectionsViewDemo />
}
